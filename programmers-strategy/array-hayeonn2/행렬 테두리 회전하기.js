function solution(new_id) {
  let result = "";



  // 2단계 : 소문자, 숫자, -, _, . 제외
  for(let i = 0; i < new_id.length; i++){
      // 1단계 : 모든 대문자를 소문자로 치환한다.
      let case1 = new_id.toLowerCase();
      let able = "abcdefghijklmnopqrstuvwxyz0123456789-_.";
      if(able.indexOf())
  }

}